# ---------------------------------------------------------------------------
import sys
from lib import share
log = share.log
# ---------------------------------------------------------------------------


# ---------------------------------------------------------------------------
def isSpecil(string: str, specil: str = "!@#$%^&*()+_|\\[]{}:;\"\'?/>.<,") -> bool:
    for char in specil:
        if (char in string):
            return True
    else:
        return False
# ---------------------------------------------------------------------------
# ---------------------------------------------------------------------------


def checkVersion(target=(3, 9, 6)) -> bool:
    return (sys.version_info.major, sys.version_info.minor,
            sys.version_info.micro) >= target
# ---------------------------------------------------------------------------
